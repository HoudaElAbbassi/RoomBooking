{
  "name": "roombooking-netlify",
  "private": true,
  "dependencies": {
    "dotenv": "^16.5.0",
    "pg": "^8.11.3"
  },
  "scripts": {
    "build": "cd nuxt-app && npm ci && npm run generate"
  }
}
